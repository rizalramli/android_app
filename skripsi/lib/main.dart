import 'package:flutter/material.dart';
import 'package:skripsi/Screens/login/login.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home:
        new LoginScreen(), //Here you can specify the screen the app starts on.
  ));
}
